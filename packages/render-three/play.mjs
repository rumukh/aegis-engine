// Convenience launcher so a human can start the dev server with one command from the repo root:
//
//   npm run build
//   node packages/render-three/play.mjs
//
// Optional flags: --port <n> (default 5173), --host <iface> (default 127.0.0.1).
import './dist/play.js';
